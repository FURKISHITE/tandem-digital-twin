name: Tandem 7/24 Streamer

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

# BURAYI EKLE: Çakışan eski botları otomatik kapatır
concurrency:
  group: tandem-stream
  cancel-in-progress: true

jobs:
  run-python:
    runs-on: ubuntu-latest
    # ... geri kalan her şey aynı kalsın ...
